# Super Sete Talo V2

Projeto Android corrigido para GitHub Actions.

Correção principal: Chaquopy exige declaração de `ndk.abiFilters`; agora o app define `arm64-v8a` e `x86_64` em `android.defaultConfig`.
